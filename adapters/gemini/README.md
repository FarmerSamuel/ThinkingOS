# Gemini Adapter

Maps ThinkingOS requests to Google's `generateContent` shape with system
instructions, serialized conversation state, and optional JSON Schema output.
The selected model is supplied by the caller.

See Google's official [`generateContent` reference](https://ai.google.dev/api/generate-content).
