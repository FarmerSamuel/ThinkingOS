---
name: make-association
description: "Create and evaluate meaningful connections between elaborated concepts and prior knowledge. ThinkingOS synthesis skill."
---

# Make Association (ThinkingOS)

Create and evaluate meaningful connections between elaborated concepts and prior knowledge.

**Thinking habit:** Generate connections, then test their relevance and strength.

Generated from the canonical package `skills/make-association/` (version 1.0.0). Do not edit this file; update the package and run `python tools/export_claude_skills.py`.

## Purpose

Target outcome: Produce validated Associations, AssociationStrengths, NovelConnections.

### In scope

- Concept.
- Association.
- PriorKnowledge.
- Relationship.
- NovelConnection.

### Out of scope

- Provider-specific execution.
- Unvalidated solution generation.

## Position in the thinking graph

Run after: `analogy`, `deep-processing`.
If a prerequisite artifact is missing from the conversation, run the prerequisite skill first or ask the user for an equivalent input. Never invent it.

- Consumes: `ElaboratedConcepts`, `StructuralMapping`, `PriorKnowledge`.
- Produces: `Associations`, `AssociationStrengths`, `NovelConnections`.

## Required inputs

| Input | Required | Description |
| --- | --- | --- |
| `elaboratedConcepts` | Yes | The elaboratedconcepts required by Make Association. |
| `structuralMapping` | Yes | The structuralmapping required by Make Association. |
| `priorKnowledge` | No | The priorknowledge required by Make Association. |

Collect required inputs before analysis. When a required input is missing or materially ambiguous, ask the single highest-impact clarification question and wait for the answer.

## Execution pipeline

Follow the shared ThinkingOS Engine order:

```text
Collect Inputs -> Validate -> Detect Missing Information -> Evaluate Elements
-> Run Logic Rules -> Evaluate Reasoning -> Generate Evaluation
-> Generate Output -> Recommend Next Skill
```

A later stage must not bypass an unresolved blocking result from an earlier stage.

## Validation gates

- **MAKEASSOCIATION-V01** — Required inputs must be present, relevant, and mutually consistent. If it fails: Return needs clarification with one prioritized question.
- **MAKEASSOCIATION-V02** — Material ambiguity and missing information must be surfaced. If it fails: Record the gap and stop dependent logic.
- **MAKEASSOCIATION-V03** — Inputs must satisfy registered dependency semantics. If it fails: Recommend the missing prerequisite.

## Logic rules

Apply in order:

1. **MAKEASSOCIATION-L01** — Identify and normalize the skill's thinking elements: Concept, Association, PriorKnowledge, Relationship, NovelConnection. If it fails: Request or record the missing element.
2. **MAKEASSOCIATION-L02** — Apply the Make Association relationship rules without introducing platform assumptions. If it fails: Record the unsupported relationship.
3. **MAKEASSOCIATION-L03** — Test contradictions, alternatives, and failure conditions. If it fails: Reduce confidence and disclose the limitation.
4. **MAKEASSOCIATION-L04** — Generate a schema-compatible evaluation and next-step recommendation. If it fails: Return incomplete rather than guessing.

## Conversation rules

- Ask one material question at a time.
- Separate facts, assumptions, and interpretations.
- Explain why a clarification affects the evaluation.
- Stop when inputs are sufficient.

## Evaluation

Score each criterion from 0 to 4:

| Criterion | Weight | Description |
| --- | --- | --- |
| Relevance Score | 0.2 | Quality of relevance in the current reasoning. |
| Strength Score | 0.2 | Quality of strength in the current reasoning. |
| Novelty Score | 0.2 | Quality of novelty in the current reasoning. |
| Evidence Score | 0.2 | Quality of evidence in the current reasoning. |
| Usefulness Score | 0.2 | Quality of usefulness in the current reasoning. |

Scale: 0 = unusable, 1 = weak, 2 = partial, 3 = sufficient, 4 = robust.

## Output format

Return one fenced JSON object that conforms to the ThinkingOS universal
output contract (`schemas/output.schema.json`), then a short plain-language
recap of the recommendation and next step.

Required fields:

- `summary` — one-paragraph result including the frame status.
- `evaluation` — object with `score`, `strengths`, `weaknesses`,
  `missingInformation`, `logicalIssues`, `recommendation`, `confidence`.
- `reasoning` — array of strings; report each rubric dimension as a structured
  entry (for example `Goal Score: 3/4 — supported by stated evidence`).
- `assumptions` — every assumption the analysis relied on.
- `missingInformation` — unknowns that could materially change the result.
- `questions` — outstanding clarification questions (at most one prioritized
  question when a blocking gap exists).
- `nextStep` — the single most useful next action.
- `confidence` — `low`, `medium`, or `high` (or a number from 0 to 1).

Preserve material uncertainty. Never hide a blocking weakness behind a strong
overall score, and never treat fluent prose as evidence of valid reasoning.

## Never rules

- Never fabricate missing information.
- Never bypass a blocking validation failure.
- Never hide uncertainty behind a score.
- Never invoke an unsatisfied downstream skill.

## Next skill transitions

Recommend at most one downstream skill:

- `mind-map` — Condition: The current output satisfies the registered transition contract. Reason: Continue reasoning with MindMap.
- `emergence` — Condition: The current output satisfies the registered transition contract. Reason: Continue reasoning with Emergence.

Never recommend a downstream skill whose prerequisites are unsatisfied. When no transition condition holds, recommend a direct action or clarification instead.
