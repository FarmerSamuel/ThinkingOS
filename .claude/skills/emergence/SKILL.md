---
name: emergence
description: "Identify system-level patterns that arise from interactions and cannot be explained by isolated parts alone. ThinkingOS systems-thinking skill."
---

# Emergence (ThinkingOS)

Identify system-level patterns that arise from interactions and cannot be explained by isolated parts alone.

**Thinking habit:** Test whether observed patterns arise from interactions before labeling them emergent.

Generated from the canonical package `skills/emergence/` (version 1.0.0). Do not edit this file; update the package and run `python tools/export_claude_skills.py`.

## Purpose

Target outcome: Produce validated EmergentPatterns, SystemBehaviors, EmergenceConditions.

### In scope

- Interaction.
- Pattern.
- SystemBehavior.
- EmergenceCondition.
- AlternativeExplanation.

### Out of scope

- Provider-specific execution.
- Unvalidated solution generation.

## Position in the thinking graph

Run after: `complexity`, `make-association`.
If a prerequisite artifact is missing from the conversation, run the prerequisite skill first or ask the user for an equivalent input. Never invent it.

- Consumes: `ComplexityProfile`, `InteractionMap`, `Associations`.
- Produces: `EmergentPatterns`, `SystemBehaviors`, `EmergenceConditions`.

## Required inputs

| Input | Required | Description |
| --- | --- | --- |
| `complexityProfile` | Yes | The complexityprofile required by Emergence. |
| `interactionMap` | Yes | The interactionmap required by Emergence. |
| `associations` | No | The associations required by Emergence. |

Collect required inputs before analysis. When a required input is missing or materially ambiguous, ask the single highest-impact clarification question and wait for the answer.

## Execution pipeline

Follow the shared ThinkingOS Engine order:

```text
Collect Inputs -> Validate -> Detect Missing Information -> Evaluate Elements
-> Run Logic Rules -> Evaluate Reasoning -> Generate Evaluation
-> Generate Output -> Recommend Next Skill
```

A later stage must not bypass an unresolved blocking result from an earlier stage.

## Validation gates

- **EMERGENCE-V01** — Required inputs must be present, relevant, and mutually consistent. If it fails: Return needs clarification with one prioritized question.
- **EMERGENCE-V02** — Material ambiguity and missing information must be surfaced. If it fails: Record the gap and stop dependent logic.
- **EMERGENCE-V03** — Inputs must satisfy registered dependency semantics. If it fails: Recommend the missing prerequisite.

## Logic rules

Apply in order:

1. **EMERGENCE-L01** — Identify and normalize the skill's thinking elements: Interaction, Pattern, SystemBehavior, EmergenceCondition, AlternativeExplanation. If it fails: Request or record the missing element.
2. **EMERGENCE-L02** — Apply the Emergence relationship rules without introducing platform assumptions. If it fails: Record the unsupported relationship.
3. **EMERGENCE-L03** — Test contradictions, alternatives, and failure conditions. If it fails: Reduce confidence and disclose the limitation.
4. **EMERGENCE-L04** — Generate a schema-compatible evaluation and next-step recommendation. If it fails: Return incomplete rather than guessing.

## Conversation rules

- Ask one material question at a time.
- Separate facts, assumptions, and interpretations.
- Explain why a clarification affects the evaluation.
- Stop when inputs are sufficient.

## Evaluation

Score each criterion from 0 to 4:

| Criterion | Weight | Description |
| --- | --- | --- |
| PatternEvidence Score | 0.2 | Quality of patternevidence in the current reasoning. |
| InteractionBasis Score | 0.2 | Quality of interactionbasis in the current reasoning. |
| LevelDistinction Score | 0.2 | Quality of leveldistinction in the current reasoning. |
| AlternativeTesting Score | 0.2 | Quality of alternativetesting in the current reasoning. |
| PredictiveUse Score | 0.2 | Quality of predictiveuse in the current reasoning. |

Scale: 0 = unusable, 1 = weak, 2 = partial, 3 = sufficient, 4 = robust.

## Output format

Return one fenced JSON object that conforms to the ThinkingOS universal
output contract (`schemas/output.schema.json`), then a short plain-language
recap of the recommendation and next step.

Required fields:

- `summary` — one-paragraph result including the frame status.
- `evaluation` — object with `score`, `strengths`, `weaknesses`,
  `missingInformation`, `logicalIssues`, `recommendation`, `confidence`.
- `reasoning` — array of strings; report each rubric dimension as a structured
  entry (for example `Goal Score: 3/4 — supported by stated evidence`).
- `assumptions` — every assumption the analysis relied on.
- `missingInformation` — unknowns that could materially change the result.
- `questions` — outstanding clarification questions (at most one prioritized
  question when a blocking gap exists).
- `nextStep` — the single most useful next action.
- `confidence` — `low`, `medium`, or `high` (or a number from 0 to 1).

Preserve material uncertainty. Never hide a blocking weakness behind a strong
overall score, and never treat fluent prose as evidence of valid reasoning.

## Never rules

- Never fabricate missing information.
- Never bypass a blocking validation failure.
- Never hide uncertainty behind a score.
- Never invoke an unsatisfied downstream skill.

## Next skill transitions

Recommend at most one downstream skill:

- `boundary` — Condition: The current output satisfies the registered transition contract. Reason: Continue reasoning with Boundary.
- `complexity` — Condition: The current output satisfies the registered transition contract. Reason: Continue reasoning with Complexity.

Never recommend a downstream skill whose prerequisites are unsatisfied. When no transition condition holds, recommend a direct action or clarification instead.
