---
name: complexity
description: "Evaluate interaction, dependency, uncertainty, and adaptation within a bounded system. ThinkingOS systems-thinking skill."
---

# Complexity (ThinkingOS)

Evaluate interaction, dependency, uncertainty, and adaptation within a bounded system.

**Thinking habit:** Examine relationships and behavior rather than counting parts alone.

Generated from the canonical package `skills/complexity/` (version 1.0.0). Do not edit this file; update the package and run `python tools/export_claude_skills.py`.

## Purpose

Target outcome: Produce validated ComplexityProfile, InteractionMap, UncertaintyDrivers.

### In scope

- Component.
- Interaction.
- Dependency.
- UncertaintyDriver.
- FeedbackLoop.

### Out of scope

- Provider-specific execution.
- Unvalidated solution generation.

## Position in the thinking graph

Run after: `break-it-down`, `boundary`.
If a prerequisite artifact is missing from the conversation, run the prerequisite skill first or ask the user for an equivalent input. Never invent it.

- Consumes: `ProblemComponents`, `SystemBoundary`, `DependencyStructure`.
- Produces: `ComplexityProfile`, `InteractionMap`, `UncertaintyDrivers`.

## Required inputs

| Input | Required | Description |
| --- | --- | --- |
| `problemComponents` | Yes | The problemcomponents required by Complexity. |
| `systemBoundary` | Yes | The systemboundary required by Complexity. |
| `dependencyStructure` | No | The dependencystructure required by Complexity. |

Collect required inputs before analysis. When a required input is missing or materially ambiguous, ask the single highest-impact clarification question and wait for the answer.

## Execution pipeline

Follow the shared ThinkingOS Engine order:

```text
Collect Inputs -> Validate -> Detect Missing Information -> Evaluate Elements
-> Run Logic Rules -> Evaluate Reasoning -> Generate Evaluation
-> Generate Output -> Recommend Next Skill
```

A later stage must not bypass an unresolved blocking result from an earlier stage.

## Validation gates

- **COMPLEXITY-V01** — Required inputs must be present, relevant, and mutually consistent. If it fails: Return needs clarification with one prioritized question.
- **COMPLEXITY-V02** — Material ambiguity and missing information must be surfaced. If it fails: Record the gap and stop dependent logic.
- **COMPLEXITY-V03** — Inputs must satisfy registered dependency semantics. If it fails: Recommend the missing prerequisite.

## Logic rules

Apply in order:

1. **COMPLEXITY-L01** — Identify and normalize the skill's thinking elements: Component, Interaction, Dependency, UncertaintyDriver, FeedbackLoop. If it fails: Request or record the missing element.
2. **COMPLEXITY-L02** — Apply the Complexity relationship rules without introducing platform assumptions. If it fails: Record the unsupported relationship.
3. **COMPLEXITY-L03** — Test contradictions, alternatives, and failure conditions. If it fails: Reduce confidence and disclose the limitation.
4. **COMPLEXITY-L04** — Generate a schema-compatible evaluation and next-step recommendation. If it fails: Return incomplete rather than guessing.

## Conversation rules

- Ask one material question at a time.
- Separate facts, assumptions, and interpretations.
- Explain why a clarification affects the evaluation.
- Stop when inputs are sufficient.

## Evaluation

Score each criterion from 0 to 4:

| Criterion | Weight | Description |
| --- | --- | --- |
| InteractionCoverage Score | 0.2 | Quality of interactioncoverage in the current reasoning. |
| DependencyQuality Score | 0.2 | Quality of dependencyquality in the current reasoning. |
| Uncertainty Score | 0.2 | Quality of uncertainty in the current reasoning. |
| Adaptation Score | 0.2 | Quality of adaptation in the current reasoning. |
| ExplanatoryPower Score | 0.2 | Quality of explanatorypower in the current reasoning. |

Scale: 0 = unusable, 1 = weak, 2 = partial, 3 = sufficient, 4 = robust.

## Output format

Return one fenced JSON object that conforms to the ThinkingOS universal
output contract (`schemas/output.schema.json`), then a short plain-language
recap of the recommendation and next step.

Required fields:

- `summary` — one-paragraph result including the frame status.
- `evaluation` — object with `score`, `strengths`, `weaknesses`,
  `missingInformation`, `logicalIssues`, `recommendation`, `confidence`.
- `reasoning` — array of strings; report each rubric dimension as a structured
  entry (for example `Goal Score: 3/4 — supported by stated evidence`).
- `assumptions` — every assumption the analysis relied on.
- `missingInformation` — unknowns that could materially change the result.
- `questions` — outstanding clarification questions (at most one prioritized
  question when a blocking gap exists).
- `nextStep` — the single most useful next action.
- `confidence` — `low`, `medium`, or `high` (or a number from 0 to 1).

Preserve material uncertainty. Never hide a blocking weakness behind a strong
overall score, and never treat fluent prose as evidence of valid reasoning.

## Never rules

- Never fabricate missing information.
- Never bypass a blocking validation failure.
- Never hide uncertainty behind a score.
- Never invoke an unsatisfied downstream skill.

## Next skill transitions

Recommend at most one downstream skill:

- `emergence` — Condition: The current output satisfies the registered transition contract. Reason: Continue reasoning with Emergence.
- `heuristic` — Condition: The current output satisfies the registered transition contract. Reason: Continue reasoning with Heuristic.

Never recommend a downstream skill whose prerequisites are unsatisfied. When no transition condition holds, recommend a direct action or clarification instead.
